const cardSystem = (balance, zone) => {
    let user
    let  max = 320
    //let zone = [1, 2, 3]
    let zone1 = ['holborn', 'earlsCourt']
    let zone2 = ['hammersmith', 'earlsCourt']
    let zone3 = ['wimbledon']

    while (balance >= max){
        let tfare = balance - max
        
        if(zone == ){
             balance -= 

        }
    }

}